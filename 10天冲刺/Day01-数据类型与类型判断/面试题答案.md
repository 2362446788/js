# Day 1 面试题答案

## 1. JavaScript 数据类型有哪些？`BigInt` 和 `Symbol` 分别解决什么问题？

标准回答：

JavaScript 现在常说有 8 种数据类型，分成两大类。

- 原始类型：`Undefined`、`Null`、`Boolean`、`Number`、`String`、`Symbol`、`BigInt`
- 引用类型：`Object`

其中数组、函数、日期对象、本质上都属于 `Object`。

`Symbol` 主要解决两个问题：

- 创建唯一值，避免属性名冲突
- 为对象定义非字符串键，常用于框架内部协议或元编程，比如 `Symbol.iterator`

`BigInt` 主要解决大整数精度问题。因为普通 `Number` 是双精度浮点数，安全整数范围是 `-(2^53 - 1)` 到 `2^53 - 1`，超出这个范围会丢精度。

面试延伸：

- `typeof null` 历史原因是 `object`
- 函数是“可调用对象”
- 数组是“特殊对象”

## 2. `typeof null` 为什么是 `object`？

标准回答：

这是 JavaScript 早期历史遗留问题。`typeof` 底层是根据二进制标记做粗粒度判断，早期实现里对象类型的标记是 `000`，而 `null` 的底层表示恰好也被判断成了这一类，所以 `typeof null` 返回了 `object`。

这不是语义上的正确分类，而是历史兼容结果。现在为了兼容旧代码，规范也没有改。

## 3. `typeof` 的局限性是什么？为什么检测数组不能只靠 `typeof`？

标准回答：

`typeof` 适合检测原始类型中的大部分值，尤其是：

- `undefined`
- `boolean`
- `number`
- `string`
- `symbol`
- `bigint`
- `function`

它的局限主要有两个：

- `null` 会返回 `object`
- 对绝大多数引用类型都只能返回 `object`，无法细分数组、日期、正则等

所以数组不能只靠 `typeof`，因为：

```js
typeof [] // 'object'
typeof {} // 'object'
```

它无法区分数组和普通对象。

## 4. `instanceof` 的原理是什么？它有哪些局限？

标准回答：

`instanceof` 的原理是沿着实例对象的原型链向上查找，看构造函数的 `prototype` 是否出现在这条链上。

例如：

```js
arr instanceof Array
```

本质是在判断 `Array.prototype` 是否出现在 `arr.__proto__` 这条链上。

局限：

- 不能检测原始类型，比如 `1 instanceof Number` 是 `false`
- 跨 iframe / 跨 realm 场景可能失效，因为不同上下文的构造函数不是同一个引用
- 如果手动改写了原型链，结果也可能变化

## 5. 如何准确判断一个值是不是数组、普通对象、日期对象？

标准回答：

最稳的方案是用：

```js
Object.prototype.toString.call(value)
```

例如：

```js
Object.prototype.toString.call([]) // [object Array]
Object.prototype.toString.call({}) // [object Object]
Object.prototype.toString.call(new Date()) // [object Date]
```

如果专门判断数组，也可以直接用：

```js
Array.isArray(value)
```

这是现代项目里最推荐的数组判断方式。

## 6. `Object.prototype.toString.call` 为什么更稳？

标准回答：

因为它基于对象内部的 `[[Class]]` 标签或内置类型标识，能更细地识别对象的具体种类，而不是像 `typeof` 一样只返回 `object`。

它更稳的原因：

- 可以区分数组、对象、日期、正则、Map、Set 等
- 不受大多数普通原型链关系干扰
- 在跨 iframe 场景下通常比 `instanceof` 更可靠

## 7. `==` 和 `===` 的区别是什么？面试里你推荐哪一个，为什么？

标准回答：

- `===` 是严格相等，不做类型转换
- `==` 是宽松相等，必要时会做隐式类型转换

例如：

```js
1 == '1'  // true
1 === '1' // false
```

实际开发里优先推荐 `===`，因为可读性更好、行为更稳定、坑更少。

但面试里不能只说“都用 `===`”，还要知道 `==` 的规则，因为很多经典题都会考它。

## 8. `[] == ![]` 为什么是 `true`？

标准回答：

分三步看：

1. 先算 `![]`

数组是对象，对象转布尔值永远是 `true`，所以：

```js
![] === false
```

2. 变成：

```js
[] == false
```

3. `==` 比较时，对象和布尔值都会参与转换：

- `false -> 0`
- `[] -> '' -> 0`

最终变成：

```js
0 == 0 // true
```

## 9. `NaN` 为什么不等于自己？如何判断一个值是不是 `NaN`？

标准回答：

`NaN` 表示“不是一个合法数字结果”。按照 IEEE 754 浮点规范，`NaN` 与任何值都不相等，包括它自己。

判断方式推荐：

```js
Number.isNaN(value)
```

不要优先用全局 `isNaN`，因为它会先做类型转换，容易误判：

```js
isNaN('abc') // true
Number.isNaN('abc') // false
```

## 10. `Number()`、`parseInt()`、`parseFloat()` 的区别是什么？

标准回答：

- `Number()`：整体转换，要求整个字符串都能转成合法数字，否则就是 `NaN`
- `parseInt()`：从左到右解析整数，遇到第一个不合法字符就停止
- `parseFloat()`：从左到右解析浮点数，支持一个小数点

例如：

```js
Number('12px') // NaN
parseInt('12px') // 12
parseFloat('12.5px') // 12.5
```

## 11. 为什么 `0.1 + 0.2 !== 0.3`？项目里怎么处理精度问题？

标准回答：

因为 JavaScript 的 `Number` 底层是 IEEE 754 双精度浮点数，很多十进制小数无法被二进制精确表示，像 `0.1` 和 `0.2` 都是近似值，运算后误差累积，所以：

```js
0.1 + 0.2 // 0.30000000000000004
```

项目处理方案：

- 金额类场景统一转成整数再算，比如“元转分”
- 使用高精度库，比如 `decimal.js`、`big.js`
- 展示层再做格式化，不在核心计算里靠 `toFixed` 硬修

## 12. `Object.is()` 和 `===` 的区别是什么？

标准回答：

绝大多数场景下结果一样，但有两个关键差异：

- `NaN`
  - `NaN === NaN` 是 `false`
  - `Object.is(NaN, NaN)` 是 `true`
- `+0` 和 `-0`
  - `+0 === -0` 是 `true`
  - `Object.is(+0, -0)` 是 `false`

所以 `Object.is` 更像“SameValue”比较。

## 13. 超长整型 ID 为什么后端常常返回字符串？前端什么时候该用 `BigInt`？

标准回答：

因为前端普通 `Number` 的安全整数范围有限，如果后端直接返回超长整型数字，前端接收后可能精度丢失。

所以很多后端会把长 ID 作为字符串返回，前端：

- 如果只是展示、传递、拼接，一律按字符串处理最安全
- 如果确实要参与大整数运算，再考虑转成 `BigInt`

注意：`BigInt` 不能和普通 `Number` 直接混算。

## 14. 说几个你见过的隐式转换坑题。

标准回答：

常见的有：

- `[] == ![]`
- `[] + {}`
- `{ } + []`
- `'5' - 2`
- `'5' + 2`
- `null == undefined`
- `[] == 0`
- `'' == 0`

面试答这题的重点不是背题，而是能拆规则。

## 15. 如何判断 `object` 为空？

标准回答：

如果是“普通对象是否没有自有可枚举属性”，常用方法有：

```js
Object.keys(obj).length === 0
```

也可以：

```js
Reflect.ownKeys(obj).length === 0
```

区别是 `Reflect.ownKeys` 会把不可枚举和 `Symbol` 键也算进去，更完整。

## 16. 强制类型转换、隐式类型转换分别有哪些规则？

标准回答：

强制类型转换是开发者主动调用 API：

- `Number()`
- `String()`
- `Boolean()`

隐式转换发生在语言运算过程中，比如：

- `+` 号拼接字符串
- `==` 宽松比较
- `if` 判断中的布尔转换
- 算术运算里的数字转换

典型规则：

- 转布尔：`0`、`''`、`null`、`undefined`、`NaN` 是 `false`
- 对象转原始值：优先 `Symbol.toPrimitive`，再 `valueOf`，再 `toString`
- `+` 遇到字符串倾向拼接
- 其它算术运算更倾向转数字

## 17. `==` 和 `===` 的区别是什么？

同第 7 题。

## 18. JavaScript 的数据类型有哪些？

同第 1 题。

## 19. JavaScript 变量在内存中的堆栈存储怎么理解？

标准回答：

面试里常见的简化表达是：

- 原始类型值通常直接保存在栈中或按值访问
- 引用类型变量在栈里保存引用地址，真正对象内容在堆里

所以对象赋值本质上复制的是地址，不是对象本身。

注意：这是前端面试中的简化模型，帮助理解“按值传递”和“共享引用”，不用讲成底层 VM 实现细节。

## 20. 如何判断 JavaScript 的数据类型？

标准回答：

一套面试最稳的答法是：

- 原始类型优先用 `typeof`
- 数组优先用 `Array.isArray`
- 精确判断对象种类用 `Object.prototype.toString.call`
- 原型关系判断用 `instanceof`

## 21. ES 每个版本引入了什么内容？

标准回答：

面试不用背全年表，重点讲关键版本：

- ES5：严格模式、`JSON`、数组迭代方法、`Object.defineProperty`
- ES6 / ES2015：`let/const`、箭头函数、模板字符串、解构、Promise、class、module、Map/Set、Symbol
- ES2016：`includes`、幂运算符
- ES2017：`async/await`
- ES2018：对象展开、`Promise.finally`
- ES2019：`flat`、`flatMap`
- ES2020：`BigInt`、可选链、空值合并、`Promise.allSettled`

## 22. `let` 声明变量有哪些特性？

标准回答：

- 块级作用域
- 不会重复声明同名变量
- 有暂时性死区
- 会提升，但提升后不能在声明前访问
- 不会像 `var` 一样挂到全局对象上

## 23. 变量提升和函数提升的优先级怎么理解？

标准回答：

在同一作用域内，函数声明的优先级高于 `var` 声明。

例如：

```js
console.log(a);
var a = 1;
function a() {}
```

创建阶段里：

- 先收集函数声明 `a`
- 再处理 `var a`，但不会覆盖已存在的函数声明

所以第一次输出是函数本身，执行阶段再被赋值为 `1`。

## 24. 如何判断对象相等？

标准回答：

要先定义“相等”的含义：

- 如果比较引用是否相同，直接用 `===`
- 如果比较内容是否一致，要做深比较

常见深比较方案：

- 简单场景：`JSON.stringify`
- 严谨场景：递归比较 key 和 value
- 工程里通常用成熟库，比如 `lodash.isEqual`

## 25. `null` 和 `undefined` 的区别是什么？

标准回答：

- `undefined` 更偏“未定义、缺省、未赋值”
- `null` 更偏“开发者主动表示空值”

例如：

- 变量声明未赋值是 `undefined`
- 函数没有显式返回值，默认返回 `undefined`
- 主动清空对象引用常写成 `null`

宽松比较里：

```js
null == undefined // true
null === undefined // false
```

## 26. 宿主对象、内置对象、原生对象分别是什么？

标准回答：

- 内置对象：ECMAScript 标准自带的对象，比如 `Object`、`Array`、`Date`、`Math`
- 宿主对象：运行环境提供的对象，比如浏览器里的 `window`、`document`，Node 里的 `global`
- 原生对象：很多资料里常和“内置对象”混用，一般指语言规范原生提供的对象

面试里说明“不同资料术语边界略有差异”会更稳。

## 27. 如何区分数组和对象？

标准回答：

推荐：

```js
Array.isArray(value)
```

或者：

```js
Object.prototype.toString.call(value) === '[object Array]'
```

不要只用 `typeof`，因为数组和对象都会返回 `object`。

## 28. 什么是类数组（伪数组），如何转成真实数组？

标准回答：

类数组是“长得像数组，但不是数组”的对象，通常具备：

- `length`
- 按数字索引访问

比如：

- `arguments`
- DOM `NodeList`

转数组常见方法：

```js
Array.from(list)
[...list]
Array.prototype.slice.call(list)
```

## 29. 创建函数的几种方式有哪些？

标准回答：

- 函数声明
- 函数表达式
- 箭头函数
- `new Function()`

其中 `new Function()` 很少在业务里用，因为它会动态解析字符串，性能和安全性都较差。

## 30. 创建对象的几种方式有哪些？

标准回答：

- 对象字面量：`{}`
- `new Object()`
- 构造函数：`new Person()`
- `Object.create(proto)`
- `class` 实例化

一般业务里最常见的是对象字面量、构造函数 / class、`Object.create`。
