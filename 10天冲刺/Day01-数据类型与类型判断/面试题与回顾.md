# Day 1 面试题与回顾

## 对应计划

- 日期：`2026-06-16`
- 主题：数据类型、类型检测、隐式转换、经典坑题
- 搭配资料：
  - [一晚彻底掌握数据类型及相互间的转换规则.html](/Users/wangtao/Desktop/resume/JS-珠峰/0406/课程大纲/一晚彻底掌握数据类型及相互间的转换规则.html)
  - [第一阶段课后作业.html](/Users/wangtao/Desktop/resume/JS-珠峰/0410/课后作业/第一阶段课后作业.html)

## 今天学完必须能说清

- JS 有哪些数据类型，原始类型和引用类型怎么分。
- `typeof`、`instanceof`、`constructor`、`Object.prototype.toString.call` 的适用场景。
- `==` 的隐式转换规则。
- `null`、`undefined`、`NaN`、`Symbol`、`BigInt` 的面试高频坑点。

## 高频面试题

1. JavaScript 数据类型有哪些？`BigInt` 和 `Symbol` 分别解决什么问题？
2. `typeof null` 为什么是 `object`？
3. `null` 和 `undefined` 的区别是什么？
4. `typeof` 的局限性是什么？为什么检测数组不能只靠 `typeof`？
5. `instanceof` 的原理是什么？它有哪些局限？
6. 如何准确判断一个值是不是数组、普通对象、日期对象？
7. `Object.prototype.toString.call` 为什么更稳？
8. `==` 和 `===` 的区别是什么？面试里你推荐哪一个，为什么？
9. `[] == ![]` 为什么是 `true`？
10. `NaN` 为什么不等于自己？如何判断一个值是不是 `NaN`？
11. `Number()`、`parseInt()`、`parseFloat()` 的区别是什么？
12. 为什么 `0.1 + 0.2 !== 0.3`？项目里怎么处理精度问题？
13. `Object.is()` 和 `===` 的区别是什么？
14. 超长整型 ID 为什么后端常常返回字符串？前端什么时候该用 `BigInt`？
15. 说几个你见过的隐式转换坑题。
15.1. 数据类型检测有哪些？

## 飞书宝典补充题

以下题目来自你提供的飞书《大前端面试宝典》中的 JavaScript 题库，适合并到今天一起练：

16. 如何判断 `object` 为空？
17. 强制类型转换、隐式类型转换分别有哪些规则？
18. `==` 和 `===` 的区别是什么？
19. JavaScript 的数据类型有哪些？
20. JavaScript 变量在内存中的堆栈存储怎么理解？
21. 如何判断 JavaScript 的数据类型？
22. ES 每个版本引入了什么内容？
23. `let` 声明变量有哪些特性？
24. 变量提升和函数提升的优先级怎么理解？
25. 如何判断对象相等？
26. `null` 和 `undefined` 的区别是什么？
27. 宿主对象、内置对象、原生对象分别是什么？
28. 如何区分数组和对象？
29. 什么是类数组（伪数组），如何转成真实数组？
30. 创建函数的几种方式有哪些？
31. 创建对象的几种方式有哪些？

## 隐式类型转换专项测试

下面这一组不要直接背答案，建议你先手写推导过程，再去对照答案文档。

32. 以下代码输出什么？

```js
let result = 100 + true + 21.2 + null + undefined + "Tencent" + [] + null + 9 + false;
console.log(result);
```

33. 以下代码输出什么？

```js
console.log([] + []);
```

34. 以下代码输出什么？

```js
console.log([] + {});
```

35. 以下代码输出什么？

```js
console.log({} + []);
```

36. 以下代码输出什么？

```js
console.log([] == false);
```

37. 以下代码输出什么？

```js
console.log({} == false);
```

38. 以下代码输出什么？

```js
console.log('5' + 3 - 2);
```

39. 以下代码输出什么？

```js
console.log('5' - 3 + 2);
```

40. 以下代码输出什么？

```js
console.log(null + 1);
```

41. 以下代码输出什么？

```js
console.log(undefined + 1);
```

42. 以下代码输出什么？

```js
console.log(null == undefined);
```

43. 以下代码输出什么？

```js
console.log(null === undefined);
```

44. 以下代码输出什么？

```js
console.log([] == ![]);
```

45. 以下代码输出什么？

```js
console.log([1, 2] == '1,2');
```

46. 以下代码输出什么？

```js
console.log([1] + [2, 3]);
```

47. 以下代码输出什么？

```js
console.log([] + 1);
```

48. 以下代码输出什么？

```js
console.log({} + 1);
```

49. 以下代码输出什么？

```js
console.log(true + true);
```

50. 以下代码输出什么？

```js
console.log(true + 'true');
```

51. 以下代码输出什么？

```js
console.log(1 < 2 < 3);
```

52. 以下代码输出什么？

```js
console.log(3 > 2 > 1);
```

53. 以下代码输出什么？

```js
console.log([] == 0);
```

54. 以下代码输出什么？

```js
console.log('' == 0);
```

55. 以下代码输出什么？

```js
console.log('' == []);
```

56. 以下代码输出什么？

```js
console.log([] === []);
```

57. 以下代码输出什么？

```js
console.log(NaN == NaN, NaN === NaN, Object.is(NaN, NaN));
```

58. 以下代码输出什么？

```js
console.log(+[]);
```

59. 以下代码输出什么？

```js
console.log(+{});
```

60. 以下代码输出什么？

```js
console.log([] ? 'A' : 'B');
```

61. 以下代码输出什么？

```js
console.log('2' > '10');
```

## 今天重点手写

- `isPlainObject`
- `plus` / `minus` 浮点数精度处理
- 手推 `arr.map(parseInt)`

## 回顾清单

- 我能不看资料说出 8 种数据类型。
- 我能解释为什么数组是对象、函数也是对象，但函数又是可调用对象。
- 我能清晰区分“显示转换”和“隐式转换”。
- 我能现场讲出一套“类型检测方案选型”：
  - 基础类型优先 `typeof`
  - 引用类型优先 `toString.call`
  - 继承关系判断用 `instanceof`
- 我能独立做完课后作业第一部分的大多数题。
- 我能说清 `let` 和 `var` 在提升、块级作用域、暂时性死区上的区别。
- 我能解释“堆内存/栈内存”这类说法在前端面试里的常见答法。

## 答题提醒

- 这一类题最怕背结论，不讲规则。
- 回答隐式转换时，尽量按“先转什么，再比什么，最后结果是什么”的顺序说。
- 遇到坑题，不要直接报答案，先说转换过程。

## 网络参考

- Front End Interview Handbook 的 JavaScript 面试题页包含 `null/undefined`、`==/===`、`this`、`hoisting` 等高频题，可作为加练题库：
  - [JavaScript Interview Questions](https://www.frontendinterviewhandbook.com/javascript-questions)
- MDN 可用来校准定义：
  - [Closures - MDN](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Guide/Closures)
  - [this - MDN](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/this)
